/**
 * Email — tables only, no CSS variables. A different register with its own laws.
 *
 * GENERATED BARREL. Import from the group, never the file:
 *   import { … } from '@ui/email';
 */

export * from './email-kit';
export * from './templates';
