/**
 * The standing kitchen — maintained only by what the cook already does.
 *
 * GENERATED BARREL. Import from the group, never the file:
 *   import { … } from '@ui/stock';
 */

export * from './stock/stock';
