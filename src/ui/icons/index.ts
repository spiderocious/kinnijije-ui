/**
 * Icon proxy. All icon imports go through `@icons`, never `lucide-react`
 * directly, so the icon library can be swapped in exactly one place.
 */
export {
  AlertCircle,
  ArrowRight,
  ArrowUpRight,
  Check,
  ChevronDown,
  ChevronLeft,
  ChevronRight,
  Cookie,
  Info,
  Loader2,
  Sparkles,
  X,
} from 'lucide-react';

export type { LucideIcon, LucideProps } from 'lucide-react';
